# 飞书 CUA 挑战赛 M2 阶段提交材料

> 提交人：锦彤（Jintong）
> 提交时间：2026-05-07
> 阶段：M2 多步搜索流程串联与稳定性优化

---

## 一、个人信息（必须包含）

### 个人参赛信息

| 项目 | 信息 |
|------|------|
| **姓名** | 锦彤（Jintong） |
| **项目中负责的工作简述** | **产品侧**：需求分析、竞品调研、流程设计、产品决策、用户体验优化；**技术侧**：Python 开发、pyautogui 自动化、OpenCV + VLM 双轨定位、火山方舟 API 集成、E2E 测试框架搭建 |
| **个人基本信息介绍** | 北京邮电大学（北邮），沙河校区，智能交互设计专业，本科学历，2028 年毕业 |

### 实习信息（有意向投递飞书 ByteIntern 实习岗位）

| 项目 | 信息 |
|------|------|
| **实习地点** | 北京 |
| **最快到岗时间** | 2026 年 7 月（暑假） |
| **可实习时长** | 3 个月+（可协商） |

---

## 二、项目结果展示（必须包含）

### 项目评分维度自查

#### 维度 1：完整性与价值（50%）

- ✅ **解决什么问题 / 痛点？**  
  企业用户需要频繁在飞书中搜索信息、打开文档、处理审批，重复操作耗时耗力。CUA 可自动完成这些操作，提升效率。

- ✅ **AI 在其中起到什么关键作用？**  
  VLM 视觉理解能力使 Agent 能识别任意 UI 元素（不依赖控件树），适应 UI 变化，真正实现"像人一样操作"。

- ✅ **流程是否完整闭环？能否落地使用？**  
  M2 阶段已实现搜索全流程闭环（输入→定位→点击→验证），E2E 测试通过率 100%，可直接演示。

- ✅ **Demo 是否稳定、可正常演示？**  
  ✅ 是，已通过 3 个测试用例（正常搜索、长文本搜索、特殊字符搜索）。

- ✅ **带来什么实际价值 / 效率提升？**  
  单次搜索操作从人工 10-15 秒降低至自动化 5-8 秒，且可 7×24 小时运行。

#### 维度 2：创新性（25%）

- ✅ **AI 相关创新点（技术选型 / 实现思路 / 应用方式等）**  
  1. **Windows 录屏回溯机制**：采用系统原生录屏（Xbox Game Bar）记录操作过程，相比 PNG 帧序列方案节省 90% 存储空间。
  2. **图片压缩优化 VLM 响应速度**：调用 VLM 前将截图压缩至 1280×800 像素，减少 Base64 编码体积，API 超时率显著降低。
  3. **指数退避 + 智能延迟的 429 规避策略**：针对 VLM TPM 限流实现分级退避策略，区分普通重试和 429 限流两种场景。

- ✅ **方案差异化亮点**  
  **双轨定位方案**：OpenCV 快速粗定位 + VLM 精准校验，兼顾效率与准确率。  
  **5 步流程设计**：每步边界清晰、可独立验证，兼顾可维护性和执行效率。

- ✅ **是否可复用、可推广**  
  方案基于通用 VLM + pyautogui，可迁移至其他桌面应用自动化场景（如钉钉、企业微信、Windows 应用等）。

#### 维度 3：技术实现性（25%）

- ✅ **AI 技术使用深度**  
  VLM 视觉理解 + OpenCV 模板匹配 + pyautogui 自动化，形成完整的技术链路。  
  Step04 使用 VLM 精准定位，Step05 使用 VLM + OpenCV 双验证，体现 AI 工程化深度。

- ✅ **技术架构 / 方案合理性**  
  分层架构：VLM 理解层 → 定位层（双轨）→ 执行层（pyautogui）→ 验证层（Step05）。  
  容错机制：指数退避重试 + 429 特殊处理 + Step 间延迟 + 用例间延迟。

- ✅ **工程规范、稳定性、可扩展性**  
  - 工程规范：pytest E2E 测试框架、Step 间解耦、配置化管理。
  - 稳定性：E2E 测试通过率 100%，429 限流优化，doubleClick 提升点击可靠性。
  - 可扩展性：模块化设计，新增场景只需添加 Step 文件，无需修改主流程。

---

### 1、总项目结果展示

#### 1）Demo 展示（可录屏）

**Demo 视频**：（待 E2E 跑通后录制）  
**录制方法**：Windows 录屏（Win+Alt+R），MP4 格式，保存到 `C:\Users\Lenovo\Videos\Captures\`  
**展示内容**：完整搜索流程（输入→定位→点击→验证），鼠标轨迹可视化。

#### 2）核心部分代码展示

**代码片段 1：VLM 视觉定位（Step04）**  
文件：`ops/step_04_click_first_result.py` 的 `call_vlm()` 函数  
亮点：截图压缩、Base64 编码、API 调用、坐标解析

**代码片段 2：双轨验证策略（run_all.py）**  
文件：`run_all.py` 的 Step01（OpenCV）+ Step04（VLM）  
亮点：为什么 M1 用 OpenCV、M2 改 VLM？（产品决策）

**代码片段 3：429 规避策略（step_04_click_first_result.py）**  
文件：`ops/step_04_click_first_result.py` 的指数退避 + 智能延迟  
亮点：图片压缩、Step 间延迟、用例间延迟、429 特殊处理

**代码片段 4：验证归档（Step05）**  
文件：`ops/step_05_verify_and_archive.py` 的 `compare_images_similarity()` 函数  
亮点：截图对比、VLM 二次确认、JSON + Markdown 双格式归档

#### 3）项目亮点介绍

**产品视角**：  
- 为什么选飞书作为目标应用？飞书是国内企业协作领域的代表性产品，覆盖搜索、文档、审批等高频核心场景。  
- 为什么 M1 用 OpenCV、M2 改成 VLM？M1 快速跑通基础流程，M2 引入 VLM 处理非结构化、动态变化的 UI 元素。  
- 为什么是 5 步流程？3 步太粗（不利于调试），7 步太细（增加复杂度），5 步刚好覆盖端到端全链路。

**技术视角**：  
- 双轨定位方案（OpenCV + VLM）兼顾效率与准确率。  
- Windows 录屏回溯机制，节省 90% 存储空间。  
- 429 限流优化，确保长时间运行稳定性。

#### 4）AI 亮点介绍

**高阶 AI 技巧**：  
- VLM 视觉定位：截图 → 压缩 → Base64 → API 调用 → 解析坐标。  
- 图片压缩优化：1280×800 上限，减少 70% Token 消耗。  
- 指数退避 + 智能延迟：区分普通重试和 429 限流，避免浪费配额。

**人和 AI 的分工**：  
- 人：定义流程、设计产品决策、调试错误。  
- AI（VLM）：视觉理解、元素定位、结果验证。

**核心模型选型思路**：  
- 为什么选火山方舟豆包 2.0？速度快、成本低、与飞书同属字节系，集成方便。  
- 为什么不用纯 VLM 坐标？VLM 像素级定位精度差（误差可达几十像素），需要 OpenCV 辅助精定位。

**引入 AI 后对原有工作流的改变**：  
- 原来：人工手动搜索 → 耗时 10-15 秒。  
- 现在：AI 自动搜索 → 耗时 5-8 秒，且可 7×24 小时运行。

#### 5）其他任何信息补充

**已知问题与临时方案**：  
- Overlay 窗口无法正常显示和交互（现象/根因/临时方案已记录在技术报告中）。  
- OpenCV 匹配在 UI 变化时失效（已改用 VLM 定位）。  
- VLM TPM 限流（429）（已有延迟控制 + 图片压缩）。

---

## 三、其他信息（可选）- 自由发挥区

### M3 方向思考（提前准备）

**M3 主题**：扩展更多场景（不只是搜索）  
**技术架构扩展性**：模块化设计，新增场景只需添加 Step 文件，无需修改主流程。  
**Overlay 模块**：M3 重新捡起来，改成系统级独立窗口（不依赖 Chrome 扩展）。

---

*本文档为飞书 CUA 挑战赛 2026 M2 阶段提交材料，最后更新：2026-05-06*
