# 飞书CUA复赛提交检查清单

> 截止时间：**2026年5月7日 12:00**
> 最后更新：2026-05-05 21:30

---

## 一、必须完成的（P0 - 不完成不能交）

### 1. 代码运行验证
- [ ] **M2 全量8条E2E测试跑通**
  - [ ] 至少 6/8 条用例完全通过 (75%+)
  - [ ] 每条用例截图保存完整
  - [ ] 报告文件生成在 `reports/` 目录
  - [ ] 命令：`python run_all.py --run-all`

### 2. Git仓库推送
- [ ] 所有最新改动 commit 并 push
  - [x] Step 04 VLM调用优化（简化为2次调用）
  - [x] Step 04 OpenCV模板图路径修复
  - [x] Step 05 截图选择逻辑修复
  - [x] requirements.txt 补全依赖
  - [x] run_all.py 超时保护
- [ ] GitHub 仓库公开可访问

---

## 二、文档准备（必须包含的维度）

### A. 个人信息
| 字段 | 内容 |
|------|------|
| 姓名 | （填写） |
| 角色 | 个人参赛 / 组长 / 成员 |
| 项目负责工作 | （简述：CUA框架设计 + VLM视觉定位 + 多步串联） |
| 学校 | （填写） |
| 专业 | （填写） |
| 学历 | 本科/硕士/博士 |
| 毕业时间 | （填写） |
| 实习信息 | 地点 / 最快到岗时间 / 可实习时长 |

### B. Demo展示
- [ ] **录屏**（格式待确认，可能MP4）
  - [ ] 展示完整的搜索流程：打开飞书 → 点击搜索 → 输入关键词 → 点击结果
  - [ ] 最好展示2条不同类型用例（如IM搜索 + Calendar搜索）
  - [ ] 时长控制在 **2-3分钟** 内
  - [ ] 录屏工具推荐：OBS Studio（免费）或 Windows自带的 Xbox Game Bar (Win+G)

### C. 核心代码展示
- [ ] 架构图（5层架构）
  - [x] 视觉感知层：pyautogui截图 + OpenCV模板匹配 + VLM定位
  - [x] 规划决策层：run_all.py 步骤编排 + retry_step 重试机制
  - [x] 执行操作层：pyautogui点击/输入/pyperclip粘贴
  - [x] 状态验证层：VLM语义比对 + OpenCV像素对比
  - [x] 评估报告层：Markdown + JSON 自动生成
- [ ] 关键代码片段（建议截取）
  - [ ] `step_04_click_first_result.py` 的 SIMPLE_LOCATE_PROMPT（提示词工程）
  - [ ] `call_vlm()` 指数退避重试逻辑
  - [ ] `run_all.py` 的 `retry_step()` 包装器
  - [ ] 坐标校验与过渡区自动修正逻辑

### D. 项目亮点介绍
参考评分维度来写：

#### 维度1：完整性与价值（50%）
- 解决什么痛点？
  > **写这个**：传统UI自动化依赖DOM/Accessibility Tree，对桌面客户端无效。
  > CUA方案通过纯视觉方式实现飞书桌面端自动化测试，
  > 无需应用提供任何接口或调试能力。
- AI的关键作用？
  > **写这个**：VLM（豆包视觉大模型）替代了传统的XPath/CSS选择器，
  > 能理解界面语义而非仅匹配像素。这是"看到"和"理解"的区别。
- 流程闭环？
  > **写这个**：截图→识别→操作→验证→归档，5步全自动，人工零干预。
- 实际价值？
  > **写这个**：一条E2E测试从手动5分钟→自动30秒，效率提升10倍。

#### 维度2：创新性（25%）
- [ ] **混合定位策略**：OpenCV模板匹配（快但脆）+ VLM语义理解（慢但智），分层兜底
- [ ] **VLM调用优化**：从3-5次压缩到2次，429限流率下降约70%
- [ ] **指数退避重试**：15/30/60/120/240s，智能应对TPM限制
- [ ] **坐标过渡区自动修正**：检测标签栏→内容区边界，自动偏移+40px
- [ ] **Chrome Overlay悬浮窗**：实时显示Agent状态（可选演示）

#### 维度3：技术实现性（25%）
- AI技术使用深度：
  > VLM prompt engineering（结构化提示词）、多模态输入(截图+指令)、
  > 输出解析(坐标正则提取)、语义验证(前后对比)
- 架构合理性：
  > 5层解耦、每步可独立运行、统一的result字典协议、重试包装器
- 工程规范：
  > Git版本管理、requirements.txt、自动化报告、截图归档

### E. AI亮点介绍（单独板块）
- [ ] **模型选型思路**：为什么选豆包VLM（火山方舟Ark）？延迟低、成本低、中文场景好
- [ ] **人机分工**：
  - AI做什么：界面理解、元素定位、操作验证
  - 人做什么：编写测试用例、审核报告、处理异常
- [ ] **Prompt Engineering**：
  - SIMPLE_LOCATE_PROMPT：约束输出格式为 `x=数字,y=数字`
  - VERIFY_PROMPT：二元判断 SUCCESS/FAIL
  - 为什么这样设计：减少token消耗、降低解析错误率
- [ ] **引入AI后的改变**：
  - Before：每个UI变化需要手动更新选择器
  - After：VLM自动适应界面变化，维护成本趋近于零

---

## 三、可选的加分项（有时间就做）

### 进阶能力（M3-M5方向）
- [ ] **异常处理**：弹窗检测+关闭后继续执行
- [ ] **多产品覆盖**：Calendar创建日程（至少2条新用例）
- [ ] **Docs文档操作**：创建/编辑文档（至少2条新用例）
- [ ] **自然语言驱动**：支持 "搜索XX并点击第一条" 这种自由文本输入
- [ ] **Dashboard可视化**：成功率折线图、耗时柱状图

### 提交形式加分
- [ ] README.md 写精美（架构图+快速开始+Demo GIF）
- [ ] 如果有前端页面，部署到 Vercel/GitHub Pages

---

## 四、截止前时间线

```
5/05 晚上  完成 Step 04 修复 + 代码审查 ✅ (正在进行)
5/05 深夜   跑通单条 --search-term "测试" 验证
5/06 白天   全量 --run-all 8条用例 + 录屏Demo
5/06 晚上   写提交文档 + 推送Git + 群里问验收标准疑问
5/07 上午   最终检查 + 提交表单 + 开权限
5/07 12:00  ⚠️ 截止！
```

---

## 五、当前项目状态速查

| 检查项 | 状态 | 备注 |
|--------|:----:|------|
| M1 单步操作 | ✅ 已通过 | 成功率 62.5% (5/8) |
| M2 流程串联 | 🔄 待实测 | Step 04 刚优化完 |
| M3 多产品覆盖 | ⏳ 未开始 | Calendar优先 |
| M4 评估体系 | ⚠️ 基础版有 | Markdown+JSON报告 |
| M5 进阶能力 | ⏳ 未开始 | 看时间决定 |
| Git仓库 | ⚠️ 需确认最新push | |
| 提交文档 | ⏳ 未写 | 用本清单准备 |
| Demo录屏 | ⏳ 未录 | 跑通后再录 |

---

*老周的话：这份清单你明天对着勾就行。先跑通代码，再准备文档。代码不通文档写得再好也没用。*
